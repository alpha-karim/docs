---
title: "Event logging"
description: "Log events from the pilot tablet or phone and Mission Overwatch, and understand what is stored."
icon: "list"
---

## Logging events on the pilot tablet or phone

On the pilot tablet or phone, operators can log events in several ways:

- Tap the quick event buttons (**Subject**, **Entry**, **Evidence**, **Support**, **Secured**, **Relay**, **Custom**).
- Use the quick event wheel for faster field tagging.
- Say `relay <event details>` when voice logging is enabled (the keyword is `relay`).
- Type a custom event line in the pilot console's manual log field, then tap **Log Manual Event**.
- Let optional local AI create auto-tagged events when configured.

Quick event presets can match what Mission Overwatch displays for the selected scenario.

## Logging events in Mission Overwatch

In Mission Overwatch, use either the **Event Logging** quick buttons (**Subject Spotted**, **Entry Cleared**, **Evidence Located**, **Officer Support**, **Area Secured**, **Relay Request**) or the **Manual Event (fast)** card, where you can type a note and click **Log Manual Event**.

## Tagging events after the mission

After a mission is closed, reviewers can still add evidence-backed events from uploaded footage.

Open **Play with events**, scrub to the relevant frame, enter an **Event name**, and click **Tag event**. AlphaRelay adds the event to the mission log, stores a screenshot from the video frame, and links the event to the same mission timeline used by the footage viewer.

This is useful when an important detail is easier to identify during post-mission review than during the live flight.

## What gets stored

Logged events are:

- Timestamped
- Added to the **Event Log**
- Associated with mission activity
- Linked to screenshots from the pilot view when the relay is connected
- Linked to footage screenshots when tagged from post-mission playback
- Labeled by source, such as quick event, manual event, voice relay, Mission Overwatch, or local AI

If the system is offline or delayed, events and screenshots may sync once connectivity returns.
